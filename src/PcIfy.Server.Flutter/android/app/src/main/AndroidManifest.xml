<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access for the local HTTP server -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Read local network interface / WiFi IP address -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Android 13+: required to post any notification (including foreground-service) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Foreground service to keep server alive in background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14+: dataSync is the correct type for a local file-transfer
         server. connectedDevice is for Bluetooth/USB/NFC device interactions
         (the wrong direction — this app IS the server, not the client). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Wake lock to keep CPU active while serving -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Lets the app ask the user to exempt it from battery optimisation (Doze).
         Without this exemption, aggressive OEMs (Samsung/Xiaomi/OPPO/Vivo) kill
         the background process even with a live foreground service.
         NOTE: for Play Store submission this permission needs Console
         justification; the in-app fallback opens the battery-optimisation
         settings list, which is always allowed. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Storage: Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <!-- Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- Broad access to user-configured source directories on Android 11+ -->
    <!-- Shown in the app as "Allow access to all files" -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        android:minSdkVersion="30"/>

    <application
        android:label="pc-ify server"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- flutter_foreground_task foreground service -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundTaskService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- flutter_foreground_task restart receiver (auto-restart on boot / crash) -->
        <receiver
            android:name="com.pravera.flutter_foreground_task.receiver.ForegroundTaskBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

</manifest>
